import 'package:flutter/material.dart';

class ConditionScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text("Foydalanish shartlariga rozimisiz?"),
        ],
      ),
    );
  }
}
